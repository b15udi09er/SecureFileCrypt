function goToMainMenu() {
    try {
      console.log('Going back to the main menu...');
      window.location.href = 'index.html'; // Assuming your main page is index.html
    } catch (error) {
      console.error('Error during going back to the main menu:', error);
    }
  }
  
  function changeAccentColor() {
    try {
      console.log('Changing accent color...');
    } catch (error) {
      console.error('Error during changing accent color:', error);
    }
  }
  
  function configurePassword() {
    try {
      console.log('Configuring password...');
    } catch (error) {
      console.error('Error during configuring password:', error);
    }
  }
  
  function configureFont() {
    try {
      console.log('Configuring font...');
    } catch (error) {
      console.error('Error during configuring font:', error);
    }
  }
  
  function configureFileLocations() {
    try {
      console.log('Configuring file locations...');
    } catch (error) {
      console.error('Error during configuring file locations:', error);
    }
  }
  