{
  "name": "securefilecrypt",
  "version": "0.0.1",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron src/main.js",
  "package": "electron-packager . SecureFileCrypt --platform=<PLATFORM> --arch=<ARCHITECTURE> --out=dist --overwrite"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^28.1.2"
  },
  "dependencies": {
    "crypto": "^1.0.1"
  }
}
