{
  "name": "securefilecrypt",
  "version": "0.0.1",
  "description": "SecureFileCrypt is a lightweight encryption and decryption tool designed for securing your sensitive files.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron src/main.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron-rebuild": "^3.2.9"
  }
}
